# Features

Domain-oriented frontend modules.

Rules:
- Keep screen, view-model, and feature components inside each domain.
- Keep generic reusable components in `src/components/shared`.
- Keep primitive UI only in `src/components/ui`.
- Prefer API calls through `src/services/api`.
