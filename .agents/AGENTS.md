# Custom Rules

- **Docker Build Rule**: Setiap selesai melakukan perubahan kode yang memerlukan build (seperti perubahan dependencies, file konfigurasi, atau struktur utama aplikasi), Anda wajib melakukan build docker (misalnya menggunakan `docker-compose build` atau `docker build`) untuk memastikan aplikasi berfungsi dengan baik di container.
